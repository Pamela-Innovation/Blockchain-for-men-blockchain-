name: CronJob - Liquidator Bot - World
on:
  schedule:
    - cron: "50 * * * *" # ie. 4:50pm, 5:50pm ... etc.
env:
  CHAIN_ID: 480
  MIN_PROFIT_THRESHOLD_USD: 0.2
  SWAP_RECIPIENT: ''
  JSON_RPC_URL: ${{ secrets.WORLD_MAINNET_JSON_RPC_URL }}
  COVALENT_API_KEY: ${{ secrets.COVALENT_API_KEY }}
  CUSTOM_RELAYER_PRIVATE_KEY: ${{ secrets.CUSTOM_RELAYER_PRIVATE_KEY }}
  CONTRACT_JSON_URL: 'https://raw.githubusercontent.com/GenerationSoftware/pt-v5-mainnet/8e1432b70c1f135966c1b70917675cd586dda7be/deployments/world/contracts.json'
  ENV_TOKEN_ALLOW_LIST: ''
  NODE_OPTIONS: "--max_old_space_size=32768"
permissions: write-all
jobs:
  runCLI:
    name: Liquidator Bot
    runs-on: ${{ matrix.os }}
    strategy:
      max-parallel: 1
      matrix:
        node: ["20.11.1"]
        os: [ubuntu-latest]
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          submodules: recursive
      
      - name: Use Node ${{ matrix.node }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}

      - name: Store current date
        id: date
        run: echo "date=$(date +'%Y-%m-%d')" >> "$GITHUB_ENV"
      
      - name: Store current time
        id: time
        run: echo "time=$(date +'%H-%M-%S_%Z')" >> "$GITHUB_ENV"

      - name: Validate required environment variables
        id: preflight
        shell: bash
        run: |
          missing=0
          echo "can_run=false" >> "$GITHUB_OUTPUT"
          for var in JSON_RPC_URL CUSTOM_RELAYER_PRIVATE_KEY CONTRACT_JSON_URL; do
            if [ -z "${!var}" ]; then
              echo "::warning::Missing required environment variable: $var"
              missing=1
            fi
          done

          if [ "$missing" -ne 0 ]; then
            echo "Skipping liquidator run because required environment variables are missing."
          else
            echo "can_run=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Run bot
        id: runBot
        if: steps.preflight.outputs.can_run == 'true'
        run: |
          npm install
          npx tsx index.ts

      - name: Push changes (if required)
        if: steps.preflight.outputs.can_run == 'true'
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: ${{ github.ref }}
          force: true

      - name: Comment Error
        id: commentError
        if: steps.preflight.outputs.can_run == 'true' && steps.runBot.outputs.exitcode == 1
        uses: actions/github-script@v5
        continue-on-error: true
        with:
          script: |
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `Liquidator bot failed for run at "$date"__"$time": Exit Code 1`
            })
