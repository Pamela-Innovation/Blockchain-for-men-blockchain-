name: CronJob - Liquidator Bot - Ethereum Mainnet
on:
  workflow_dispatch: # Run manually only
env:
  CHAIN_ID: 1
  MIN_PROFIT_THRESHOLD_USD: 90
  SWAP_RECIPIENT: ''
  JSON_RPC_URL: ${{ secrets.ETHEREUM_MAINNET_JSON_RPC_URL }}
  COVALENT_API_KEY: ${{ secrets.COVALENT_API_KEY }}
  CUSTOM_RELAYER_PRIVATE_KEY: ${{ secrets.CUSTOM_RELAYER_PRIVATE_KEY }}
  CONTRACT_JSON_URL: 'https://raw.githubusercontent.com/GenerationSoftware/pt-v5-mainnet/ca2acf967a962355b921fd4893306b961e7bc62a/deployments/ethereum/contracts.json'
  ENV_TOKEN_ALLOW_LIST: ''
  NODE_OPTIONS: "--max_old_space_size=32768"
permissions: write-all
jobs:
  runCLI:
    name: Liquidator Bot
    runs-on: ${{ matrix.os }}
    strategy:
      max-parallel: 1
      matrix:
        node: ["20.11.1"]
        os: [ubuntu-latest]
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          submodules: recursive
      
      - name: Use Node ${{ matrix.node }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}

      - name: Store current date
        id: date
        run: echo "date=$(date +'%Y-%m-%d')" >> "$GITHUB_ENV"
      
      - name: Store current time
        id: time
        run: echo "time=$(date +'%H-%M-%S_%Z')" >> "$GITHUB_ENV"


      - name: Check required secrets
        id: checkSecrets
        run: |
          if [ -z "$JSON_RPC_URL" ] || [ -z "$CUSTOM_RELAYER_PRIVATE_KEY" ]; then
            echo "Required secrets (JSON_RPC_URL, CUSTOM_RELAYER_PRIVATE_KEY) are not configured. Skipping bot run."
            echo "secrets_configured=false" >> "$GITHUB_OUTPUT"
          else
            echo "secrets_configured=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Run bot
        id: runBot
        if: steps.checkSecrets.outputs.secrets_configured == 'true'
        run: |
          npm install
          npx tsx index.ts

      - name: Push changes (if required)
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: ${{ github.ref }}
          force: true
